---
import { Image } from 'astro:assets';
import { ui } from '../i18n/ui.js';
import { defaultLocale, localeInfo } from '../i18n/locales.js';
import { facts } from '../data/facts.js';
import boatImg from '../assets/Bilde-Fjorled-JMT-1-e1593513975750.jpg';

interface Props {
  lang?: string;
  title: string;
}

// Title and intro come from the page's content file, like every other page;
// this component owns only the board itself and its controls.
const { lang = defaultLocale, title } = Astro.props;

// Page strings and the strings the client script needs both come from the
// language's UI catalog, so a new language needs no code change here.
const t = ui(lang).ferry;
const boardStrings = { ...t.board, locale: localeInfo(lang).intl };

---

<div class="page page-wide dep-page" data-lang={lang} data-strings={JSON.stringify(boardStrings)}>
  <div class="dep-intro">
    <h1 class="page-title">{title}</h1>
    <slot />
    <div class="dep-nav">
      <button class="dep-nav-btn" id="dep-prev" disabled aria-label={t.prevDay}>
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M15 18l-6-6 6-6"/></svg>
      </button>
      <p class="dep-date-line" id="dep-date" aria-live="polite"></p>
      <button class="dep-nav-btn" id="dep-next" aria-label={t.nextDay}>
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><path d="M9 18l6-6-6-6"/></svg>
      </button>
      <span class="dep-cal-wrap">
        <button class="dep-nav-btn" id="dep-cal" aria-label={t.pickDate} aria-expanded="false" aria-haspopup="dialog">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="5" width="18" height="16" rx="2"/><path d="M3 10h18M8 3v4M16 3v4"/></svg>
        </button>
        <div class="dep-cal-pop" id="dep-cal-pop" role="dialog" aria-label={t.pickDate} hidden></div>
      </span>
    </div>
  </div>

  <div class="dep-boat-wrap">
    <Image class="dep-boat-img" src={boatImg} alt={t.boatAlt} width={800} widths={[400, 800]} sizes="280px" layout="none" />
  </div>

  <div class="dep-columns">
    <section class="dep-direction dep-to-rovar" aria-labelledby="dep-header-to-rovar">
      <h2 class="dep-header" id="dep-header-to-rovar">Haugesund → Røvær</h2>
      <div id="from-haugesund"></div>
    </section>
    <section class="dep-direction dep-to-haugesund" aria-labelledby="dep-header-to-haugesund">
      <h2 class="dep-header" id="dep-header-to-haugesund">Røvær → Haugesund</h2>
      <div id="from-rovar"></div>
    </section>
  </div>

  <p class="dep-booking">
    <svg class="dep-booking-icon" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M6.62 10.79a15.05 15.05 0 0 0 6.59 6.59l2.2-2.2a1 1 0 0 1 1.01-.24c1.12.37 2.33.57 3.58.57a1 1 0 0 1 1 1V20a1 1 0 0 1-1 1A17 17 0 0 1 3 4a1 1 0 0 1 1-1h3.5a1 1 0 0 1 1 1c0 1.25.2 2.46.57 3.58a1 1 0 0 1-.25 1.01l-2.2 2.2z"/></svg> {t.legendPre}
    <a href={facts.kolumbusBookingUrl} target="_blank" rel="noopener">{facts.kolumbusBookingHost}</a>
    {t.legendPost}
  </p>

  <p class="dep-credit">
    {t.creditPre} <a href="https://entur.no/" target="_blank" rel="noopener">Entur</a>{t.creditPost}
    <span class="dep-kolumbus-sep">|</span>
    <a id="kolumbus-link" href="https://reise.kolumbus.no/no/search?fromId=NSR:StopPlace:25940&toId=NSR:StopPlace:26090" target="_blank" rel="noopener">{t.kolumbusLabel}</a> {t.kolumbusTail}
  </p>
</div>

<script src="../scripts/departures.js"></script>

<style>
  .dep-page {
    display: grid;
    grid-template-columns: 1fr 280px;
    column-gap: 2rem;
    align-items: start;
    padding-top: 1rem;
  }

  .dep-intro {
    grid-column: 1;
    grid-row: 1;
    align-self: center;
  }

  .dep-boat-wrap {
    grid-column: 2;
    grid-row: 1;
    align-self: center;
    width: 280px;
    height: 180px;
    border-radius: 10px;
    overflow: hidden;
  }

  .dep-boat-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: 55% 45%;
    scale: 1.4;
  }

  .dep-columns,
  .dep-booking,
  .dep-credit {
    grid-column: 1 / -1;
  }

  @media (max-width: 640px) {
    .dep-page {
      grid-template-columns: 1fr;
    }
    .dep-intro { order: 1; grid-column: auto; grid-row: auto; }
    .dep-columns { order: 2; grid-column: auto; }
    .dep-boat-wrap {
      order: 3;
      grid-column: auto;
      grid-row: auto;
      width: 100%;
      height: 160px;
    }
    .dep-booking { order: 4; }
    .dep-credit { order: 5; }
    .dep-nav {
      justify-content: center;
    }
  }

  .dep-nav {
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  .dep-nav-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 2rem;
    height: 2rem;
    border: 1px solid var(--color-border);
    border-radius: 6px;
    background: var(--color-foam);
    color: var(--color-ocean);
    cursor: pointer;
    padding: 0;
    transition: background 0.15s, opacity 0.15s;
  }

  .dep-nav-btn:hover:not(:disabled) {
    background: var(--color-sand);
  }

  .dep-nav-btn:disabled {
    opacity: 0.3;
    cursor: default;
  }

  .dep-nav-btn svg {
    width: 1.1rem;
    height: 1.1rem;
  }

  .dep-cal-wrap {
    position: relative;
    display: inline-flex;
  }

  .dep-cal-pop {
    position: absolute;
    top: calc(100% + 0.4rem);
    left: 50%;
    transform: translateX(-50%);
    z-index: 20;
    width: 17rem;
    padding: 0.6rem;
    background: var(--color-foam);
    border: 1px solid var(--color-border);
    border-radius: 10px;
    box-shadow: 0 6px 20px rgba(0, 0, 0, 0.12);
    transition: opacity 0.18s ease, transform 0.18s ease, visibility 0s;
  }

  /* [hidden] stays the state the script reads, but collapse it with
     visibility rather than display so the popup can animate both ways. */
  .dep-cal-pop[hidden] {
    display: block;
    opacity: 0;
    visibility: hidden;
    transform: translateX(-50%) translateY(-0.35rem);
    transition: opacity 0.18s ease, transform 0.18s ease,
      visibility 0s linear 0.18s;
  }

  /* The calendar body is built in departures.js, so its elements never carry
     the scope attribute. Same :global() treatment as the departure rows. */

  .dep-cal-pop :global(.dep-cal-head) {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.25rem;
    margin-bottom: 0.4rem;
  }

  .dep-cal-pop :global(.dep-cal-month) {
    font-size: 0.9rem;
    font-weight: 600;
    color: var(--color-ocean-dark);
  }

  .dep-cal-pop :global(.dep-cal-nav) {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 1.75rem;
    height: 1.75rem;
    border: none;
    border-radius: 6px;
    background: none;
    color: var(--color-ocean);
    cursor: pointer;
    padding: 0;
  }

  .dep-cal-pop :global(.dep-cal-nav:hover:not(:disabled)) {
    background: var(--color-sand);
  }

  .dep-cal-pop :global(.dep-cal-nav:disabled) {
    opacity: 0.25;
    cursor: default;
  }

  .dep-cal-pop :global(.dep-cal-nav svg) {
    width: 1rem;
    height: 1rem;
  }

  .dep-cal-pop :global(.dep-cal-grid) {
    display: grid;
    grid-template-columns: repeat(7, 1fr);
    gap: 2px;
  }

  .dep-cal-pop :global(.dep-cal-wd) {
    text-align: center;
    font-size: 0.65rem;
    font-weight: 600;
    text-transform: uppercase;
    color: var(--color-text-light);
    padding-bottom: 0.25rem;
  }

  .dep-cal-pop :global(.dep-cal-day) {
    aspect-ratio: 1;
    border: none;
    border-radius: 6px;
    background: none;
    font: inherit;
    font-size: 0.8rem;
    color: var(--color-text);
    cursor: pointer;
    padding: 0;
  }

  .dep-cal-pop :global(.dep-cal-day:hover:not(:disabled)) {
    background: var(--color-sand);
  }

  .dep-cal-pop :global(.dep-cal-day:disabled) {
    opacity: 0.25;
    cursor: default;
  }

  .dep-cal-pop :global(.dep-cal-day.is-today:not(.is-selected)) {
    box-shadow: inset 0 0 0 1px var(--color-ocean);
  }

  .dep-cal-pop :global(.dep-cal-day.is-selected) {
    background: var(--color-ocean);
    color: #fff;
    font-weight: 700;
  }

  .dep-date-line {
    font-size: 1rem;
    color: var(--color-text-light);
    margin-bottom: 0;
  }

  .dep-columns {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.25rem;
    margin-top: 1rem;
    margin-bottom: 1.25rem;
  }

  @media (max-width: 640px) {
    .dep-columns { grid-template-columns: 1fr; }
    .dep-direction :global(.dep-row) {
      flex-wrap: wrap;
    }
    .dep-direction :global(.dep-info) {
      margin-left: auto;
    }
  }

  .dep-direction {
    background: var(--color-foam);
    border: 1px solid var(--color-border);
    border-radius: 10px;
    overflow: hidden;
  }

  .dep-header {
    padding: 0.85rem 1.25rem;
    font-weight: 600;
    font-size: 1rem;
    color: #fff;
    text-align: center;
    background: var(--color-ocean);
  }

  /* The list is rebuilt on every day change and 60s refresh. Rows fade up in
     a short stagger so the swap reads as a transition rather than a blink. */
  @keyframes dep-row-in {
    from {
      opacity: 0;
      transform: translateY(0.35rem);
    }
  }

  .dep-direction :global(.dep-list) {
    list-style: none;
    padding: 0.25rem 0;
  }

  .dep-direction :global(.dep-list li) {
    padding: 0.55rem 1.25rem;
    border-bottom: 1px solid var(--color-sand-dark);
    animation: dep-row-in 0.35s ease both;
  }

  .dep-direction :global(.dep-list li:nth-child(2)) { animation-delay: 0.04s; }
  .dep-direction :global(.dep-list li:nth-child(3)) { animation-delay: 0.08s; }
  .dep-direction :global(.dep-list li:nth-child(4)) { animation-delay: 0.12s; }
  .dep-direction :global(.dep-list li:nth-child(5)) { animation-delay: 0.16s; }
  .dep-direction :global(.dep-list li:nth-child(6)) { animation-delay: 0.2s; }
  .dep-direction :global(.dep-list li:nth-child(n + 7)) { animation-delay: 0.24s; }

  .dep-direction :global(.dep-row) {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 0.75rem;
  }

  .dep-direction :global(.dep-detail) {
    display: grid;
    grid-template-rows: 0fr;
    transition: grid-template-rows 0.25s ease, margin 0.25s ease, opacity 0.2s ease;
    margin-top: 0;
    opacity: 0;
  }

  .dep-direction :global(.dep-detail.open) {
    grid-template-rows: 1fr;
    margin-top: 0.4rem;
    opacity: 1;
  }

  .dep-direction :global(.dep-detail-inner) {
    overflow: hidden;
    min-height: 0;
    background: var(--color-sand);
    border-radius: 6px;
    font-size: 0.8rem;
    color: var(--color-text);
    line-height: 1.4;
  }

  .dep-direction :global(.dep-detail.open .dep-detail-inner) {
    padding: 0.5rem 0.75rem;
  }

  .dep-direction :global(.dep-list li:last-child) {
    border-bottom: none;
  }

  .dep-direction :global(.dep-route) {
    display: flex;
    align-items: baseline;
    flex-wrap: wrap;
    gap: 0.15rem 0.3rem;
    min-width: 0;
    flex: 1 1 0;
  }

  .dep-direction :global(.dep-time) {
    font-size: 1.05rem;
    font-weight: 600;
    font-variant-numeric: tabular-nums;
    color: var(--color-text);
  }

  .dep-direction :global(.dep-arrow) {
    color: var(--color-text-light);
    font-size: 0.85rem;
  }

  .dep-direction :global(.dep-arr) {
    font-size: 0.95rem;
    font-weight: 400;
    font-variant-numeric: tabular-nums;
    color: var(--color-text-light);
  }

  .dep-direction :global(.dep-via) {
    font-size: 0.75rem;
    color: var(--color-text-light);
  }

  .dep-direction :global(.dep-info) {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-shrink: 0;
  }

  .dep-direction :global(.dep-duration) {
    font-size: 0.8rem;
    color: var(--color-text-light);
    font-variant-numeric: tabular-nums;
    white-space: nowrap;
  }

  .dep-direction :global(.dep-notice) {
    cursor: pointer;
    display: inline-flex;
    align-items: center;
    border: none;
    font-family: inherit;
  }

  .dep-direction :global(.dep-booking-notice) {
    background: var(--color-kelp);
    color: #fff;
    border-radius: 4px;
    padding: 0.2rem 0.4rem;
    gap: 0.25rem;
    font-size: 0.7rem;
    font-weight: 600;
    white-space: nowrap;
  }

  .dep-direction :global(.dep-booking-notice svg) {
    width: 0.85rem;
    height: 0.85rem;
  }

  .dep-direction :global(.dep-info-notice) {
    background: var(--color-ocean-light);
    color: #fff;
    border-radius: 4px;
    padding: 0.2rem 0.4rem;
    gap: 0.25rem;
    font-size: 0.7rem;
    font-weight: 600;
    white-space: nowrap;
  }

  .dep-direction :global(.dep-info-notice svg) {
    width: 0.85rem;
    height: 0.85rem;
  }

  .dep-direction :global(.passed .dep-time),
  .dep-direction :global(.passed .dep-arrow),
  .dep-direction :global(.passed .dep-arr),
  .dep-direction :global(.passed .dep-via),
  .dep-direction :global(.passed .dep-duration) {
    opacity: 0.35;
  }

  .dep-direction :global(.next) {
    background: color-mix(in srgb, var(--color-ocean) 10%, transparent);
    border-left: 4px solid var(--color-ocean);
    padding-left: calc(1.25rem - 4px);
  }

  .dep-direction :global(.next .dep-time) {
    color: var(--color-ocean-dark);
    font-size: 1.25rem;
    font-weight: 700;
  }

  .dep-direction :global(.next .dep-arr) {
    color: var(--color-text);
  }

  /* Own line, so a long via list can never crowd it on a narrow screen. */
  .dep-direction :global(.dep-countdown) {
    margin: 0.15rem 0 0;
    font-size: 0.8rem;
    font-weight: 600;
    color: var(--color-ocean);
    font-variant-numeric: tabular-nums;
  }

  .dep-direction :global(.dep-loading),
  .dep-direction :global(.dep-empty),
  .dep-direction :global(.dep-error) {
    padding: 1.5rem 1.25rem;
    text-align: center;
    color: var(--color-text-light);
    font-size: 0.9rem;
  }

  .dep-direction :global(.dep-error) {
    color: #c0392b;
  }

  .dep-booking {
    font-size: 0.85rem;
    color: var(--color-text-light);
    margin-top: 0.25rem;
    margin-bottom: 0.25rem;
    display: flex;
    align-items: flex-start;
    gap: 0.3rem;
    flex-wrap: wrap;
  }

  .dep-booking-icon {
    width: 1rem;
    height: 1rem;
    color: var(--color-kelp);
    flex-shrink: 0;
    margin-top: 0.15rem;
  }

  .dep-credit {
    font-size: 0.85rem;
    color: var(--color-text-light);
  }

  .dep-kolumbus-sep {
    margin: 0 0.3rem;
    opacity: 0.4;
  }

  @media (max-width: 640px) {
    .dep-booking {
      display: block;
      line-height: 1.5;
      text-align: center;
      margin-top: 1rem;
      margin-bottom: 1rem;
    }
    .dep-booking-icon {
      display: inline-block;
      vertical-align: -0.15rem;
    }
    .dep-credit {
      line-height: 1.5;
      text-align: center;
    }
    .dep-kolumbus-sep {
      display: none;
    }
    .dep-credit a:last-child::before {
      content: "\A";
      white-space: pre;
    }
  }
</style>
